# NetBeez MCP Server Configuration

# BeezKeeper instance URL (required)
NETBEEZ_BASE_URL=https://demo1.netbeezcloud.net

# API key from Dashboard > Settings > API Keys (required)
NETBEEZ_API_KEY=your-api-key-here

# SSL verification (optional, default: true)
# Set to false for on-prem instances with self-signed certificates
NETBEEZ_SSL_VERIFY=true

# MCP transport mode (optional, default: stdio)
# Options: stdio, http, both
MCP_TRANSPORT=stdio

# HTTP transport port (optional, default: 3000)
MCP_HTTP_PORT=3000
